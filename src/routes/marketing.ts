// STUB - replaced by step S7 (marketing routes: /, /a/:slug, /legal/*, /sample-report.pdf, /sitemap.xml, /robots.txt, /embed.js, /healthz).
import express from "express";

export const router = express.Router();
